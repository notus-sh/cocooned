<%= form_with(model: list, local: true, id: form_id) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <h3>Items</h3>
  <%= render partial: "lists/forms/#{form_partial || 'basic'}", locals: { form: form } %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
