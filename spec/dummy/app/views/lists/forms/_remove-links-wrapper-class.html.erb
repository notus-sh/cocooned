<div class="nested-form">
  <%= form.fields_for :items do |item_form| %>
    <%= render 'lists/wrapped_item_fields', :form => item_form %>
  <% end %>

  <div class='links'>
    <%= link_to_add_association 'Add item', form, :items,
                                form_name: :form,
                                partial: 'lists/wrapped_item_fields'
    %>
  </div>
</div>
